/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["cambodia-me-servir.adpc.net", "localhost"],
    },
}

module.exports = nextConfig
